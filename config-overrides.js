/* config-overrides.js */

// Override the Webpack config to use when compiling your react app
module.exports = function override(config, env) {
  //do stuff with the webpack config...
  return config;
}
